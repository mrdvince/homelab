# base application template - shared across all clusters
applicationDefaults: &defaults
  project: default
  namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ValidateSpec=false

applications: {}
