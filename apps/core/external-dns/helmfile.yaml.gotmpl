environments:
  aion:
    values:
      - ../../../infrastructure/images/config.yaml
      - ../../../infrastructure/images/images/**/*.yaml
    secrets:
      - ../../../secrets/secrets.enc.yaml
  athos:
    values:
      - ../../../infrastructure/images/config.yaml
      - ../../../infrastructure/images/images/**/*.yaml
    secrets:
      - ../../../secrets/secrets.enc.yaml
---
repositories:
  - name: zot
    url: zot.home.mrdvince.me/helm
    oci: true
    username: {{ .Values.zot.charon_username }}
    password: {{ .Values.zot.charon_password }}

releases:
  - name: adguard-auth
    namespace: external-dns
    chart: zot/internal/secret
    version: 0.1.0
    values:
      - fullnameOverride: adguard-auth-secret
        data:
          url: {{ .Values.adhome.url }}
          user: {{ .Values.adhome.user }}
          password: {{ .Values.adhome.password }}

  - name: external-dns
    namespace: external-dns
    chart: zot/upstream/external-dns/external-dns
    version: 1.21.1
    needs:
      - external-dns/adguard-auth
    values:
      - values.yaml.gotmpl
