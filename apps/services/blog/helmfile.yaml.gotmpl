environments:
  aion:
    values:
      - domain: aion.home.mrdvince.me
    secrets:
      - ../../../secrets/secrets.enc.yaml
---
repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: blog
    namespace: blog
    chart: bitnami/ghost
    version: 24.0.3
    values:
      - values.yaml.gotmpl
