global:
{{ if not (.Values | get "renderStockImages" false) }}
  imageRegistry: {{ .Values.imageRegistry }}
{{ end }}
  tolerations: &longhornTolerations
    - operator: Exists
  nodeSelector: &longhornNodeSelector
    node.home.mrdvince.me/longhorn: "true"

persistence:
  defaultClass: true
  defaultClassReplicaCount: {{ .Values.defaultReplicaCount }}

defaultSettings:
  defaultReplicaCount: {{ .Values.defaultReplicaCount }}
  taintToleration: ":"
  systemManagedComponentsNodeSelector: "node.home.mrdvince.me/longhorn:true"
  concurrentAutomaticEngineUpgradePerNodeLimit: 1
  replicaAutoBalance: best-effort
  storageOverProvisioningPercentage: 200

longhornManager:
  nodeSelector: *longhornNodeSelector

longhornDriver:
  nodeSelector: *longhornNodeSelector

longhornUI:
  nodeSelector: *longhornNodeSelector

preUpgradeChecker:
  jobEnabled: false

metrics:
  serviceMonitor:
    enabled: false

csi:
  attacherReplicaCount: 1
  provisionerReplicaCount: 1
  resizerReplicaCount: 1
  snapshotterReplicaCount: 1

ingress:
  enabled: true
  ingressClassName: traefik
  host: longhorn.{{ .Values.domain }}
  tls: true
  tlsSecret: wildcard-home-mrdvince-me-tls
  path: /
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: traefik-authentik-forward-auth@kubernetescrd

{{ if not (.Values | get "renderStockImages" false) }}
image:
  longhorn:
    manager:
      registry: {{ .Values.imageRegistry }}
    engine:
      registry: {{ .Values.imageRegistry }}
    instanceManager:
      registry: {{ .Values.imageRegistry }}
    shareManager:
      registry: {{ .Values.imageRegistry }}
    ui:
      registry: {{ .Values.imageRegistry }}
  csi:
    attacher:
      registry: {{ .Values.imageRegistry }}
    provisioner:
      registry: {{ .Values.imageRegistry }}
    resizer:
      registry: {{ .Values.imageRegistry }}
    snapshotter:
      registry: {{ .Values.imageRegistry }}
    nodeDriverRegistrar:
      registry: {{ .Values.imageRegistry }}
    livenessProbe:
      registry: {{ .Values.imageRegistry }}
{{ end }}
